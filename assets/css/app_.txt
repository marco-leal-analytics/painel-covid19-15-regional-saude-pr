/* Dark theme inspired by GitHub Primer and modern tech company UIs */

@import url('https://fonts.googleapis.com/css2?family=Oswald:wght@300;400;600;700&family=Inter:wght@300;400;600;700&display=swap');
:root{
	/* Core palette */
	--bg: #0B1220; /* page background */
	--surface: #0F1724; /* slightly lighter panels */
	--muted-surface: #11151b;
	--text: #E6EEF8;
	--muted: #9AA6B2;
	--accent: #58A6FF; /* GitHub-like blue */
	--accent-2: #7DD3FC;
	--success: #2BD4A7;
	--danger: #F85149;
	--divider: rgba(255,255,255,0.06);
	--glass: rgba(255,255,255,0.03);
	--radius: 10px;
}
html[data-theme='light']{
	--bg: #ffffff;
	--surface: #f8fafc;
	--text: #0f1724;
	--muted: #6b7280;
	--accent: #2563EB;
}

html,body{height:100%;}
body{margin:0;font-family:Inter, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;background:var(--bg);color:var(--text);-webkit-font-smoothing:antialiased;-moz-osx-font-smoothing:grayscale}
/* Global helpers */
.container{max-width:1200px;margin:0 auto;padding:20px}

/* Cards and surfaces */
.ui-card, .kpi-card{background:linear-gradient(180deg,var(--surface),var(--muted-surface));color:var(--text);border-radius:var(--radius);padding:16px;border:1px solid var(--divider);box-shadow:0 8px 30px rgba(2,6,23,0.6)}
.ui-card:hover{transform:translateY(-6px);box-shadow:0 16px 40px rgba(2,6,23,0.75)}
.badge{background:var(--glass);color:var(--text);padding:6px 8px;border-radius:8px;font-size:13px}

/* Navbar / header global styles */
.navbar, .navbar-default, .page-nav, header, .bs-navbar{background:var(--surface);border-bottom:1px solid var(--divider);color:var(--text)}
.navbar a, .navbar .navbar-brand, .page-nav a { color: var(--text); }
.navbar .navbar-brand { font-weight:700; }
.page-header, .app-header { background: transparent; color: var(--text); }

.btn-primary{background:linear-gradient(90deg,var(--accent),var(--accent-2));color:#031024;border:none;padding:8px 14px;border-radius:8px;font-weight:600}
.btn-primary:hover{filter:brightness(1.05)}

.link{color:var(--accent)}
.link:hover{text-decoration:underline}
/* Resume two-column layout */
.resume-wrapper{display:grid;grid-template-columns:420px 1fr;gap:24px;padding:24px;align-items:start;max-width:1400px;margin:18px auto}
.resume-sidebar{background:linear-gradient(180deg,var(--muted-surface),var(--surface));color:var(--text);padding:20px;border-radius:12px;border:1px solid var(--divider)}
.resume-photo{width:250px;height:250px;border-radius:50%;overflow:hidden;margin:0 auto 14px;border:8px solid rgba(255,255,255,0.06);box-shadow:0 14px 40px rgba(2,6,23,0.75)}
.resume-photo img{width:100%;height:100%;object-fit:cover;display:block}
.sidebar-section{margin-top:8px}
.sidebar-section h4{margin:0 0 10px 0;font-family:Oswald,Inter,sans-serif;font-weight:600;letter-spacing:0.6px;color:var(--muted);font-size:12px}
.sidebar-links a{color:var(--accent);display:block;margin-bottom:6px;text-decoration:none}
.sidebar-contact{font-size:14px;color:var(--text)}

.resume-main{background:transparent;padding:32px;border-radius:12px}
/* Dark theme inspired by GitHub Primer and modern tech company UIs */

@import url('https://fonts.googleapis.com/css2?family=Oswald:wght@300;400;600;700&family=Inter:wght@300;400;600;700&display=swap');

:root{
	/* Core palette */
	--bg: #0B1220; /* page background */
	--surface: #0F1724; /* slightly lighter panels */
	--muted-surface: #11151b;
	--text: #E6EEF8;
	--muted: #9AA6B2;
	--accent: #58A6FF; /* GitHub-like blue */
	--accent-2: #7DD3FC;
	--success: #2BD4A7;
	--danger: #F85149;
	--divider: rgba(255,255,255,0.06);
	--glass: rgba(255,255,255,0.03);
	--radius: 10px;
}

html[data-theme='light']{
	--bg: #ffffff;
	--surface: #f8fafc;
	--text: #0f1724;
	--muted: #6b7280;
	--accent: #2563EB;
}

html,body{height:100%;}
body{margin:0;font-family:Inter, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;background:var(--bg);color:var(--text);-webkit-font-smoothing:antialiased;-moz-osx-font-smoothing:grayscale}

/* Global helpers */
.container{max-width:1200px;margin:0 auto;padding:20px}

/* Cards and surfaces */
.ui-card, .kpi-card{background:linear-gradient(180deg,var(--surface),var(--muted-surface));color:var(--text);border-radius:var(--radius);padding:16px;border:1px solid var(--divider);box-shadow:0 8px 30px rgba(2,6,23,0.6)}
.ui-card:hover{transform:translateY(-6px);box-shadow:0 16px 40px rgba(2,6,23,0.75)}
.badge{background:var(--glass);color:var(--text);padding:6px 8px;border-radius:8px;font-size:13px}

.btn-primary{background:linear-gradient(90deg,var(--accent),var(--accent-2));color:#031024;border:none;padding:8px 14px;border-radius:8px;font-weight:600}
.btn-primary:hover{filter:brightness(1.05)}

.link{color:var(--accent)}
.link:hover{text-decoration:underline}

/* Resume two-column layout */
.resume-wrapper{display:grid;grid-template-columns:420px 1fr;gap:32px;padding:28px;align-items:start;max-width:1400px;margin:20px auto}
.resume-sidebar{background:linear-gradient(180deg,var(--muted-surface),var(--surface));color:var(--text);padding:28px;border-radius:12px;border:1px solid var(--divider)}
.resume-photo{width:250px;height:250px;border-radius:50%;overflow:hidden;margin:0 auto 18px;border:8px solid rgba(255,255,255,0.06);box-shadow:0 14px 40px rgba(2,6,23,0.75)}
.resume-photo img{width:100%;height:100%;object-fit:cover;display:block}
.sidebar-section{margin-top:8px}
.sidebar-section h4{margin:0 0 8px 0;font-family:Oswald,Inter,sans-serif;font-weight:600;letter-spacing:0.6px;color:var(--muted);font-size:12px}
.sidebar-links a{color:var(--accent);display:block;margin-bottom:6px;text-decoration:none}
.sidebar-contact{font-size:14px;color:var(--text)}

.resume-main{background:transparent;padding:32px;border-radius:12px}
.resume-header{display:flex;flex-direction:column;gap:6px;margin-bottom:18px}
.resume-name{font-family:Oswald,Inter,sans-serif;font-size:44px;letter-spacing:0.6px;color:var(--text);font-weight:700}
.resume-title{font-size:14px;color:var(--muted);text-transform:uppercase}
.section-title{font-family:Oswald,Inter,sans-serif;font-size:15px;color:var(--text);border-bottom:1px solid var(--divider);padding-bottom:12px;margin-bottom:18px;margin-top:6px}

/* Timeline */
.timeline{display:flex;flex-direction:column}
.timeline-item{display:grid;grid-template-columns:140px 1fr;gap:12px;align-items:flex-start;padding-bottom:18px;position:relative}
.timeline-item:before{content:"";position:absolute;left:calc(140px - 6px);top:16px;width:12px;height:12px;background:var(--accent);border-radius:50%;box-shadow:0 0 0 6px rgba(88,166,255,0.08)}
.timeline-date{color:var(--muted);font-size:13px;text-align:right;padding-right:6px;white-space:nowrap}
.timeline-content{color:var(--text)}
.timeline-content h5{margin:0 0 6px 0;font-size:16px;color:var(--text);font-weight:600}
.timeline-content .meta{font-size:13px;color:var(--muted);margin-bottom:8px}

/* Projects list */
.resume-main .projects-list div{margin-bottom:12px}

/* Competencies & chips */
.chip{background:rgba(255,255,255,0.04);color:var(--text);padding:6px 10px;border-radius:999px;font-size:13px;margin-right:6px;margin-bottom:6px;display:inline-block}

.resume-sidebar p, .resume-sidebar li{color:var(--muted);white-space:pre-wrap;word-break:break-word;hyphens:auto;line-height:1.55}

/* Justify sidebar text so edges align with margins */
.resume-sidebar p, .sidebar-section p, .resume-sidebar li { text-align: justify; text-justify: inter-word; }

/* Icons inside sidebar */
.icon { display:inline-block; width:18px; height:18px; vertical-align:middle; margin-right:4px; color:var(--accent-2); }
.icon svg { width:100%; height:100%; fill:currentColor }
.no-wrap { white-space: nowrap; }

/* Sidebar row: place Contatos and Links side-by-side. Keep icons+text on one line */
.sidebar-row{display:flex;gap:12px;align-items:center;margin-bottom:6px}
.sidebar-row .sidebar-section{flex:1;margin-top:0}
.sidebar-row .sidebar-section h4{margin-bottom:2px}

/* Ensure each contact/link line keeps icon and text on a single row */
.sidebar-section .contact-row, .sidebar-section .link-row{display:flex;gap:6px;align-items:center}
.sidebar-section a, .sidebar-section .no-wrap{white-space:nowrap;display:inline-flex;align-items:center;gap:6px}

/* Two-column layout for Competências & Tech Stack */
.two-col-row{display:flex;gap:20px;align-items:flex-start;margin-top:6px}
.two-col-item{flex:1;min-width:180px}
.two-col-item .section-subtitle{font-size:13px;color:var(--muted);margin-bottom:6px;font-weight:600}
.competencies-list, .stack-items{margin:0;padding-left:18px;color:var(--text)}
.competencies-list li, .stack-items li{margin-bottom:6px}

/* Tech stack single-line bullets */
.tech-stack-list{margin:0;padding-left:18px;color:var(--text)}
.tech-stack-list li{margin-bottom:8px}
.tech-stack-list .section-subtitle{font-weight:600;color:var(--muted);margin-right:8px}
.tech-stack-list .stack-inline{color:var(--text)}

/* Three-column layout: Tech Stack | Competências | Certificações */
.three-col-row{display:flex;gap:18px;align-items:flex-start;margin-top:6px}
.three-col-item{flex:1;min-width:160px}
.certifications-list{margin:0;padding-left:18px;color:var(--text)}
.certifications-list li{margin-bottom:6px}

/* Footer keywords */
.keywords-footer{margin-top:28px;border-top:1px solid var(--divider);padding-top:18px}
.keywords-footer .section-small{color:var(--muted);font-size:13px;line-height:1.6}

/* Home header image */
.home-header-container{display:flex;justify-content:center;padding:12px 0;background:transparent}
.home-header-img{width: 1280px;height:140px;max-width:100%;border-radius:8px;margin-bottom:18px;display:block;background:var(--surface)}

/* Projects two-column layout */
.projects-layout{display:grid;grid-template-columns:320px 1fr;gap:20px;margin:12px 0}
.projects-sidebar{background:linear-gradient(180deg,var(--muted-surface),var(--surface));padding:12px;border-radius:10px;border:1px solid var(--divider);max-height:72vh;overflow:auto}
.projects-sidebar .project-item{padding:8px 10px;border-radius:6px;margin-bottom:6px}
.projects-sidebar .project-item a{color:var(--text);text-decoration:none}
.projects-sidebar .project-item a:hover{color:var(--accent)}
.projects-detail{background:transparent;padding:6px}
.project-detail-header h3{margin-top:0}
.project-embed{margin-top:12px}



/* Responsive */
@media (max-width:900px){.resume-wrapper{grid-template-columns:1fr;padding:16px}.resume-sidebar{order:2}.resume-main{order:1}
	.two-col-row{flex-direction:column}
	.three-col-row{flex-direction:column}
}

/* Sticky and utility styles */
.sticky-side-nav{position:fixed;right:20px;top:160px;z-index:9998;display:flex;flex-direction:column;gap:8px}
.sticky-side-nav a{background:rgba(255,255,255,0.03);color:var(--text);padding:8px 10px;border-radius:8px;text-decoration:none;font-size:13px}

#back-to-top{position:fixed;right:24px;bottom:24px;z-index:10000;display:none;background:var(--accent);color:#031024;padding:10px 12px;border-radius:999px;box-shadow:0 6px 18px rgba(0,0,0,0.3);cursor:pointer}

html{scroll-behavior:smooth}

/* Small tweaks for forms / lists */
.chip + .chip{margin-left:6px}
.section-small{font-size:13px;color:var(--muted)}

/* Skill bars */
.skill-bar{background:rgba(255,255,255,0.03);border-radius:6px;height:10px;overflow:hidden}
.skill-fill{height:100%;background:linear-gradient(90deg,var(--accent),var(--accent-2))}

.timeline-date{color:var(--muted);font-size:13px;text-align:right;padding-right:6px;white-space:nowrap}

@media (max-width:700px){.resume-wrapper{grid-template-columns:1fr}.resume-photo{width:180px;height:180px}}
.timeline-date{color:var(--muted);font-size:13px;text-align:right;padding-right:6px;white-space:nowrap}

.timeline-content{color:var(--main-text)}

.timeline-content h5{margin:0 0 6px 0;font-size:16px;color:var(--accent);font-weight:600}
