################################################################################################################
######################################## MODULO: SOBRE (SERVER) ###############################################
################################################################################################################
# Aba "Sobre" e um documento Quarto estatico, sem controles reativos.

sobreServer <- function(id) {
  moduleServer(id, function(input, output, session) {})
}
